# /x-refactor

Refactors code for better readability and maintainability.

## Usage
```
/x-refactor
```

## Example
Nested ifs → early returns, loops → reduce, extract functions.

## Workflow
`/x-test` → `/x-refactor` → `npm test` → `/x-commit`
