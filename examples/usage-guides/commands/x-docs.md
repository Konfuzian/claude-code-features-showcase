# /x-docs

Generates documentation with JSDoc/docstrings.

## Usage
```
/x-docs
```

## Example
Adds parameter descriptions, return values, and usage examples to functions.

## Workflow
Implement → `/x-test` → `/x-docs` → `/x-commit`
