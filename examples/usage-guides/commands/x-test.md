# /x-test

Generates comprehensive test suites.

## Usage
```
/x-test
```

## Example
Code: `function add(a, b) { return a + b; }`

Generates tests for positive, negative, edge cases.

## Workflow
Implement → `/x-test` → `npm test` → `/x-commit`
