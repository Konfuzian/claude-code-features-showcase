# /x-commit

Auto-stages changes and generates descriptive commit messages.

## Usage
```
/x-commit
```

## Example
Changes: Added auth → Commit: `feat: Add JWT authentication`

##  Workflow
Make changes → `/x-test` → `npm test` → `/x-commit`
