[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --browser chromium
    -m "not e2e"

# Playwright options
markers =
    e2e: comprehensive end-to-end tests (not run by default, use -m e2e to run)
